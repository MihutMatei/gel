package kronos.project.util

/**
 * JS/Web implementation: language is handled by the browser / Compose resources.
 * We keep this as a no-op so common code can call it.
 */
actual fun changeLanguage(languageCode: String) {
    // no-op
}

