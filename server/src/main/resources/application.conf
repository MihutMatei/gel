ktor {
  deployment {
    port = 8080
    port = ${?PORT}
  }
  application {
    modules = [ kronos.project.ApplicationKt.module ]
  }
}

database {
  initOnStartup = true
  url = "jdbc:postgresql://localhost:5432/hackitall"
  user = "postgres"
  password = "postgres"
  maxPoolSize = 10
}

